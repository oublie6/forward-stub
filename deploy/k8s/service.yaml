apiVersion: v1
kind: Service
metadata:
  name: forward-stub-udp
spec:
  selector:
    app: forward-stub
  ports:
    - name: udp-in
      protocol: UDP
      port: 19000
      targetPort: 19000
  type: ClusterIP
